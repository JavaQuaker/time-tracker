logging:
  level:
    root: WARN
    org:
      springframework:
        # web: DEBUG
        security: DEBUG

spring:
  # mvc:
  #   log-request-details: true
  #   log-resolved-exception: true
  # codec:
  #   log-request-details: true
  jpa:
    generate-ddl: true
    show-sql: true
  output:
    ansi:
      enabled: always


server:
  address: 0.0.0.0


  springdoc:
    swagger-ui:
      path: swagger-ui.html